name: Aggiornamento ESPN

on:
  schedule:
    - cron: "* * * * *"
  workflow_dispatch:

jobs:
  aggiorna:
    runs-on: ubuntu-latest

    steps:
      - name: Chiama API Vercel
        run: curl -f "https://100serieaserieb-api-calcio.vercel.app/api/cron/aggiorna-espn"
